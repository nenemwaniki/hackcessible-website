# Hackcessible Adjustment Branch Handover — 2026-07-24

## Scope and local setup

Work on this pass was limited to **Home** and **Cohort 2026**. People and Contact were intentionally left for a later pass.

- Reference workspace: `E:\Hackcessible\Website`, served on `http://localhost:8000`.
- Adjustment workspace: `E:\Hackcessible\Website\.worktrees\adjustments-images`, branch `adjustments-images-latest`, served on `http://localhost:8001`.
- Port 8001 had been serving the wrong directory during the session. It was restarted so it now serves the adjustment worktree with `python -m http.server 8001`.
- Use `agent-browser.cmd` in PowerShell for browser checks. The `agent-browser` PowerShell shim can be blocked by the execution policy.
- No Vercel configuration or deployment command exists in this worktree. Treat the Vercel/agent-browser tooling as local verification support, not proof of a deployment.

## User-approved direction

The user asked for these Home and Cohorts refinements:

1. Let “Assistive technology that fits real lives.” span the main content area rather than appearing narrowly centred.
2. Match the 8001 destination cards to the measured 8000 reference sizes.
3. Make the two Cohort project images compact and balanced, without a large black area beneath them.
4. Keep a moving TimeKeeper/Sawa pill toggle. Only that toggle remains sticky; case content, including “The core problem,” scrolls normally.
5. Make Moments images smaller/denser to allow future additions.

## Changes made in this pass

### `index.html`

- Removed the forced line break from “Assistive technology that fits real lives.”

### `experience-updates.css`

- Made the programme introduction a single full-width column and removed the heading width cap.
- Set the destination grid to the captured 8000 desktop dimensions:
  - Cohorts: `1139.0625px × 339.453125px`
  - People and Student interest: `560.53125px × 372.609375px`
- Preserved a single-column mobile layout.
- Set project cards to `16 / 9` with no oversized fixed minimum height.
- Converted `.case-switch` into a sticky pill and added the `.case-switch-indicator` transition layer.
- Made `.case-panel aside` static, so case copy scrolls naturally.
- Made Moments use denser minimum-width grid tracks and non-stretched figures.

### `cohorts.html`

- Added `<span class="case-switch-indicator" id="caseSwitchIndicator">` to the TimeKeeper/Sawa control.

### `site.js`

- Added `syncCaseIndicator()`, which calculates the active switch button’s width and horizontal position on load, click, and resize.

### `tests/home-cohorts-contract.test.mjs`

- Added source-level regression tests for the Home card/headline contract and the Cohort sticky-pill/non-sticky-copy contract.

## Verification already completed

From `E:\Hackcessible\Website\.worktrees\adjustments-images`:

```powershell
node --test tests/home-cohorts-contract.test.mjs
node --check site.js
```

Both commands exited successfully on 2026-07-24.

Browser checks on port 8001 confirmed at the desktop reference viewport:

- homepage headline width: `1180px` with no horizontal overflow;
- card dimensions: `1139.0625 × 339.453125`, `560.53125 × 372.609375`, and `560.53125 × 372.609375`;
- project cards: equal `580 × 326.25` (16:9);
- `.case-switch` is `sticky` and `.case-panel aside` is `static`.

## Important worktree state

The worktree was already dirty. Do not discard or overwrite unrelated changes.

Current modified/untracked paths include:

- `cohorts.html`
- `index.html`
- `people.html` — not changed in this pass; leave it alone unless the user asks for People work
- `site.js`
- `experience-updates.css`
- `tests/`

Before changing anything, inspect the diff:

```powershell
git -c safe.directory=E:/Hackcessible/Website/.worktrees/adjustments-images status --short
git -c safe.directory=E:/Hackcessible/Website/.worktrees/adjustments-images diff -- cohorts.html index.html people.html site.js experience-updates.css tests
```

## Suggested continuation

The user explicitly said to finish Home and Cohorts first, then move on to People. The next agent should:

1. Ask the user whether they want to review the current 8001 Home/Cohorts result before starting People.
2. If moving to People, inspect the current `people.html` and its existing uncommitted changes first; do not infer that they are disposable.
3. Keep port 8000 as the visual reference and port 8001 as the adjustment target.
4. Restart port 8001 from the adjustment worktree if it ever serves the wrong page:

```powershell
$targetServer = Start-Process -WindowStyle Hidden -PassThru python -ArgumentList '-m','http.server','8001' -WorkingDirectory 'E:\Hackcessible\Website\.worktrees\adjustments-images'
```

If port 8001 is occupied, identify and stop only the confirmed local static-server process before starting the replacement. Do not stop an unknown process by port number without checking it first.
